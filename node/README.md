# HTTPS Everywhere Node.js component

You can start the server with `node server.js`. You can edit the configuration of the server (e.g., add a password for the database) by modifying `configuration.json`.
